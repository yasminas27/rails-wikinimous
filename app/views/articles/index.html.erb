<h1> <%= link_to new_article_path do %>
<i class="fas fa-plus"> Add a new article </i>
<% end %>
</h1>
<% @articles.each do |article| %>
<ul class="unstyled-list">
  <li>
  <%= link_to(article.title.capitalize, article_path(article)) %>
  </li>
</ul>
<% end %>
